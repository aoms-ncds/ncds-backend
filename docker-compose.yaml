version: '1'

services:
  iet-backend:
    build:
      context: .
      dockerfile: Dockerfile.production
    image: node:21-alpine3.17
    network_mode: bridge
    container_name: iet-backend
    restart: always
    # volumes:
    #   - ./:/app
    # - ./
    # - /docker-globals/.pnpm-store:/app/.pnpm-store
    # - ../../docker-globals/.pnpm-store:/app/.pnpm-store
